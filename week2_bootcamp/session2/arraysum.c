# include <stdio.h>
# include <string.h>

int main () {

    int i;
    int sum = 0;
    int arraysum[5]; 
    arraysum = [1,2,3,4,5];
    for (i = 0; i < strlen(arraysum[5]); i++) {
        sum = sum + arraysum[i];
    }

    return(0);
}